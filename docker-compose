version: '3'
services:
  app:
    build:
      context: .
    volumes:
      - smb-share:/app/server/images

volumes:
  smb-share:
    driver_opts:
      type: cifs
      o: "username=your_username,password=your_password,iocharset=utf8,vers=3.0"
      device: //smb-server/share-name
